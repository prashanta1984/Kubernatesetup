spring:
  cloud:
    config:
      uri: ${discountms.configserverurl:http://configserver:8888}
      fail-fast: false
  application:
    name: discountms
  profiles:    active: dev

server:  port: 0